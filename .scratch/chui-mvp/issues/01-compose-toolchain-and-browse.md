> *This was generated by AI during to-tickets.*

# 01 — Compose 工具链打通 + 分类浏览

**What to build:** 自建壳用 Kotlin + Jetpack Compose 编译运行；打开 App 即显示分类列表（取自首个配置源）。修复 AGP9 内置 Kotlin 与 Compose 插件冲突。

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] `gradlew :app:assembleDebug` BUILD SUCCESSFUL，产出 `app-mobile-debug.apk`。
- [ ] 安装后打开显示分类列表；源不可用时显示为空/占位且不崩溃。
