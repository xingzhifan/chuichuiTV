> *This was generated by AI during to-tickets.*

# 04 — 多源聚合 / 播放故障切换 / 失败降级

**What to build:** 播放时聚合多个源的候选播放地址（优先级排序），某一候选失败自动切换到下一候选；失败源降低优先级（降级）。这是"抗源失效"的核心。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] 播放聚合多源候选队列，失败按序切换。
- [ ] JVM 单测：候选队列优先级排序 + 失败切下一候选 + 失败源降级。
