> *This was generated by AI during to-tickets.*

# 02 — 片源列表 → 详情(选集/换线路) → ExoPlayer 播放

**What to build:** 点分类→片源列表→点片源进详情（显示"线路 | 集"）→点某集用 ExoPlayer 播放（m3u8）。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 编译成功；装后点分类→看到片源列表，点片源→详情显示线路/集。
- [ ] 点某集→ExoPlayer 播放（用真实 m3u8）。
- [ ] JVM 单测覆盖 详情 的 `$$$/#/$` 拆解（线路/集/播放地址）。
