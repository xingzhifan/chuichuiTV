> *This was generated by AI during to-tickets.*

# 03 — 源适配接口 + Maccms/JS 适配器

**What to build:** 统一 `SourceAdapter`(home/category/detail/search) 接口；`MaccmsAdapter`（苹果 CMS 采集 API）与 `JsSpiderAdapter`（JS 蜘蛛）实现；`SourceFactory` 按源 type 创建适配器，输出同一领域模型（片源→线路→播放地址）。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] `SourceAdapter` 接口 + `MaccmsAdapter`/`JsSpiderAdapter` + `SourceFactory` 齐备。
- [ ] JVM 单测：两适配器返回统一领域模型，`$$$/#/$` 拆解正确。
