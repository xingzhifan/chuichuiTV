# Build artifacts
build/
.gradle/
*.apk

# Local config
local.properties

# IDE
.idea/
*.iml

# Vendored / abandoned fork clone (kept on disk as reference, not part of this project)
# NOTE: leading slash anchors to repo root — plain "chuichui/" also ignored the
# com/chuichui Java package directory at any depth (sources were silently untracked).
/chuichui/

# OS
.DS_Store
Thumbs.db

# Machine-specific SDK setup scripts
setup-android-sdk.ps1
assemble-android-sdk.ps1
