# 锤锤影视 (ChuiChui Movie-Watching Client)

一个跑在 Android 手机与 Android TV（小米电视）上的影视播放客户端。它不内置任何内容，而是作为“空壳”，通过用户导入的订阅配置去调用第三方内容源，聚合、搜索并播放这些源提供的视频。

## Language

**片源 (Title)**:
一部电影或一部连续剧（单部作品）。所有浏览、搜索、收藏都以“片源”为最小可寻址单位。
_Avoid_: 影片, 视频, 内容, 资源

**电影 (Movie)**:
一种片源类型，单集播放。

**剧集 (Series)**:
一种片源类型，由多集（Episode）组成。

**集 (Episode)**:
剧集下的一个可播放单元。

**源 (Source)**:
一个第三方内容提供方（通常是资源站采集 API 或 TVBox 风格配置），能给出片源目录并把某个片源解析为若干播放地址。
_Avoid_: 接口, 订阅, 线路（不要把“源”和“线路”混用）

**分类 (Category)**:
一个源给出的片源目录分组（苹果 CMS `class[]` 的 `type_id/type_name`），是浏览片源的入口。分类属于源，不是源本身。
_Avoid_: 类型, 栏目, Site（不要把“分类”当作“源”）

**线路 (Line)**:
某个源内部的一条播放途径 / 清晰度分组。一个源可提供多条线路，每条线路把片源的某一集解析到一个播放地址。
_Avoid_: 清晰度, 路径（“清晰度”是线路的一种常见形态，但不是线路本身）

**播放地址 (Play URL)**:
某集在某条线路上最终可播放的媒体地址（m3u8 或 mp4）。
_Avoid_: 链接, 播放源

**订阅配置 (Subscription)**:
用户导入的一份配置，声明一组“源”。应用是空壳，内容完全由用户导入的订阅配置决定；批量导入（粘贴 JSON 或订阅 URL）一次引入一组源，按 `api` 去重、可选覆盖/追加。一个 Subscription = 一组 Source 的声明实体。
_Avoid_: 接口, 配置源, 订阅链接

**空壳 (Shell)**:
应用本身不包含任何内容源；其功能是解析订阅配置、聚合各源、浏览与播放。这是本应用的设计属性，用于划清内容责任边界。
_Avoid_: 播放器（空壳是“应用形态”，播放器只是其中一部分）

## Notes

- 依赖：`片源 —(经源归属)→ 源 → 线路 → 播放地址`，`剧集` 还下辖 `集`。
- 多个 `源` 覆盖同一 `片源` 时，应用采用多源冗余，偏好健康度高的源。
